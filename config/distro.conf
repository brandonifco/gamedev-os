# Single source of truth for build-time identity.
# Rename the distro here and it propagates through the build.

DISTRO_NAME="GameDevOS"      # working title — rename freely
DISTRO_ID="gamedevos"        # lowercase slug used in paths
DISTRO_VERSION="0.1.0"
DISTRO_CODENAME="ignition"

# Base
BASE_DISTRO="ubuntu"
BASE_SUITE="noble"           # Ubuntu 24.04 LTS
BASE_MIRROR="http://archive.ubuntu.com/ubuntu/"
ARCH="amd64"

# Desktop / shell
DESKTOP="xfce4"
DEFAULT_SHELL="/usr/bin/fish"

# Optional components
INCLUDE_WARDEN="false"       # AI-agent sandbox layer (WIP) — opt-in; see components/warden/

# Toolchains
DOTNET_CHANNEL="10.0"        # LTS — ships natively in Ubuntu 24.04 repos (verified 2026-09)
GODOT_VERSION="4.7.2"        # .NET/C# build, latest stable (verified 2026-09)
NVIDIA_BRANCH="580"          # current default series in Ubuntu 24.04 (verified 2026-09)
NVIDIA_OPEN="true"           # prefer -open modules; REQUIRED for RTX 50-series, fine for RTX 20+
